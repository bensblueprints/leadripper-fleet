{
  "name": "leadripper-fleet",
  "version": "1.5.0",
  "description": "LeadRipper fleet control server — dispatches scrape jobs to owned worker nodes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
